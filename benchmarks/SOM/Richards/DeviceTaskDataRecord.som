DeviceTaskDataRecord = RBObject (
    |pending|

    pending = (^pending)

    pending: packet = (pending := packet)

    create = (pending := RBObject NoWork)
----
    create = (
        ^super new create
    )
)
